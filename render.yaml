services:
  - type: web
    name: multimodal-deepfake-backend
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: gunicorn backend.api.main:app -k uvicorn.workers.UvicornWorker --workers 1 --timeout 120 --bind 0.0.0.0:$PORT
